{
  "name": "@videre/monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "deploy": "pnpm --parallel -r run deploy",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "^20.2.3",
    "typescript": "^5.0.4",
    "wrangler": "^4.105.0"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/videre-project/videre-project.git",
    "directory": "."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@10.28.2"
}
