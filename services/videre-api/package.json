{
	"name": "videre-api",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy src/index.ts",
		"db:generate-types": "node scripts/generate-db-types.mjs",
		"db:check-types": "node scripts/generate-db-types.mjs --check",
		"test": "node --test --experimental-strip-types test/*.test.mjs test/*.test.ts"
	},
	"dependencies": {
		"@videre/sql-builder": "workspace:*",
		"itty-router": "^5.0.16",
		"postgres": "^3.4.4"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20231025.0"
	}
}
